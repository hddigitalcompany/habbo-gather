# Em desenvolvimento local, deixe assim (o partykit dev roda em 127.0.0.1:1999):
NEXT_PUBLIC_PARTYKIT_HOST=127.0.0.1:1999

# Depois do "npx partykit deploy", troque para o host que ele te der, por exemplo:
# NEXT_PUBLIC_PARTYKIT_HOST=habbo-gather.seu-usuario.partykit.dev
