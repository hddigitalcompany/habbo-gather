node_modules
.next
.partykit
.env.local
.DS_Store
*.log

# dados de runtime do servidor de chat (server/chatStore.js) -- histórico
# de conversas + arquivos enviados no chat, gerados no servidor rodando,
# não fazem parte do código
/data
/uploads

# aponta a pasta de origem das artes de cabelo pra fora do projeto (ver
# scripts/avatarAssetsConfig.mjs) -- caminho específico da máquina, não
# faz sentido versionar
/avatar-assets.local.json
*.tsbuildinfo
